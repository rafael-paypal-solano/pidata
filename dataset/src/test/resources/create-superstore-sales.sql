CREATE TABLE pidata_test.superstore_sales(
	ROW_ID BIGINT NOT NULL,
	ORDER_ID BIGINT NOT NULL,
	ORDER_DATE DATE NOT NULL,
	ORDER_PRIORITY INT NOT NULL,
	ORDER_QUANTITY INT NOT NULL,
	SALES DECIMAL(12,2) NOT NULL,
	DISCOUNT DECIMAL(12,2) NOT NULL,
	SHIPMODE VARCHAR(30) NOT NULL,
	PROFIT DECIMAL(12,2) NOT NULL,
	UNIT_PRICE DECIMAL(12,2) NOT NULL,
	SHIPPING_COST  DECIMAL(12,2) NOT NULL,
	CUSTOMER_NAME VARCHAR(50) NOT NULL,
	PROVINCE VARCHAR(30) NOT NULL,
	REGION VARCHAR(30) NOT NULL,
	CUSTOMER_SEGMENT VARCHAR(30) NOT NULL,
	PRODUCT_CATEGORY VARCHAR(30) NOT NULL,
	PRODUCT_SUB_CATEGORY VARCHAR(30) NOT NULL,
	PRODUCT_NAME VARCHAR(30) NOT NULL,
	PRODUCT_CONTAINER VARCHAR(30) NOT NULL,
	PRODUCT_BASE_MARGIN  DECIMAL(12,2) NOT NULL,
	SHIP_DATE DATE NOT NULL

)